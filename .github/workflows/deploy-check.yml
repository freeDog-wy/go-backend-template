name: Deployment Connectivity Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ssh-connectivity:
    name: Verify Cloudflare SSH Access
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: go-cms

    steps:
      - name: Install cloudflared
        run: |
          curl --fail --silent --show-error --location \
            --output "$RUNNER_TEMP/cloudflared" \
            https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod 755 "$RUNNER_TEMP/cloudflared"
          "$RUNNER_TEMP/cloudflared" --version

      - name: Configure SSH client
        env:
          DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          printf '%s\n' \
            'Host ssh.elseif.site' \
            '  User ubuntu' \
            '  IdentityFile ~/.ssh/deploy_key' \
            "  ProxyCommand $RUNNER_TEMP/cloudflared access ssh --hostname %h" \
            '  StrictHostKeyChecking yes' \
            '  UpdateHostKeys no' \
            '  UserKnownHostsFile ~/.ssh/known_hosts' \
            '  BatchMode yes' > ~/.ssh/config
          chmod 600 ~/.ssh/config

      - name: Verify remote shell and Docker Compose
        env:
          TUNNEL_SERVICE_TOKEN_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          TUNNEL_SERVICE_TOKEN_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: |
          timeout 60s ssh ssh.elseif.site '
            set -eu
            test "$(whoami)" = ubuntu
            printf "Connected as %s on %s\\n" "$(whoami)" "$(hostname)"
            docker --version
            docker compose version
          '
